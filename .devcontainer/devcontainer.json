{
    "name": "ICP Message Board Contract",
    "image": "ghcr.io/fxgst/icp-dev-env-azle:11",
    "forwardPorts": [
        4943
    ],
    "portsAttributes": {
        "4943": {
            "label": "dfx",
            "onAutoForward": "ignore"
        }
    },
    "onCreateCommand": "nohup bash -c 'dfx start &'",
    "updateContentCommand": "sed -i 's/npx azle message_board/npx azle message_board --native-compilation/g' dfx.json",
    "postAttachCommand": "([ -f nohup.out ] && (npm i && cat nohup.out && rm nohup.out)) || exit 0",
    "remoteUser": "root"
}